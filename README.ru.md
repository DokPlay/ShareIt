# ShareIt

[English version](README.md)

**ShareIt** — это RESTful веб-сервис для шаринга вещей между пользователями. Пользователи могут добавлять вещи для аренды, искать доступные предметы, бронировать их на определённые периоды и оставлять отзывы после использования.

## 🚀 Возможности

### Пользователи
- Создание, обновление, удаление пользователей
- Валидация уникальности email

### Вещи
- CRUD операции с проверкой владельца
- Полнотекстовый поиск по доступным вещам (название + описание)
- Комментарии от пользователей, завершивших бронирование

### Бронирования
- Создание запросов на бронирование доступных вещей
- Подтверждение/отклонение владельцем
- Фильтрация по состоянию: `ALL`, `CURRENT`, `PAST`, `FUTURE`, `WAITING`, `REJECTED`
- Просмотр бронирований как арендатор или как владелец
- Информация о последнем/следующем бронировании для владельцев

## 🛠 Технологии

| Технология | Версия | Назначение |
|------------|--------|------------|
| Java | 17+ | Язык программирования |
| Spring Boot | 3.5.x | Фреймворк приложения |
| Spring Data JPA | 3.x | Работа с базой данных |
| PostgreSQL | 16 | Продакшн база данных |
| H2 | 2.x | База для тестирования |
| Hibernate | 6.x | ORM |
| Lombok | 1.18.x | Сокращение boilerplate |
| Maven | 3.9.x | Сборка проекта |
| Docker | 20+ | Контейнеризация |
| Swagger/OpenAPI | 3.1 | Документация API |

## 📋 Требования

- JDK 17 или выше
- Docker (для PostgreSQL)
- Maven 3.9+ (или используйте встроенный `mvnw`)

## 🏃 Быстрый старт

### 1. Запуск базы данных PostgreSQL

```bash
docker run -d \
  --name shareit-postgres \
  -e POSTGRES_DB=shareit \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  postgres:16-alpine
```

### 2. Сборка приложения

```bash
./mvnw clean package -DskipTests
```

### 3. Запуск приложения

```bash
java -jar target/shareit-0.0.1-SNAPSHOT.jar
```

Или через Maven:
```bash
./mvnw spring-boot:run
```

### 4. Доступ к приложению

| Сервис | URL |
|--------|-----|
| API | http://localhost:8080 |
| Swagger UI | http://localhost:8080/swagger-ui/index.html |
| OpenAPI JSON | http://localhost:8080/v3/api-docs |

## 📡 API Эндпоинты

### Пользователи
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/users` | Получить всех пользователей |
| GET | `/users/{id}` | Получить пользователя по ID |
| POST | `/users` | Создать пользователя |
| PATCH | `/users/{id}` | Обновить пользователя |
| DELETE | `/users/{id}` | Удалить пользователя |

### Вещи
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/items` | Получить вещи владельца |
| GET | `/items/{id}` | Получить вещь по ID |
| GET | `/items/search?text=` | Поиск доступных вещей |
| POST | `/items` | Создать вещь |
| PATCH | `/items/{id}` | Обновить вещь |
| POST | `/items/{id}/comment` | Добавить комментарий |

### Бронирования
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/bookings` | Получить бронирования пользователя |
| GET | `/bookings/{id}` | Получить бронирование по ID |
| GET | `/bookings/owner` | Получить бронирования для вещей владельца |
| POST | `/bookings` | Создать запрос на бронирование |
| PATCH | `/bookings/{id}?approved=` | Подтвердить/отклонить бронирование |

> **Примечание:** Все эндпоинты `/items` и `/bookings` требуют заголовок `X-Sharer-User-Id`.

## 🧪 Тестирование

### Запуск юнит-тестов
```bash
./mvnw test
```

### Запуск API-тестов (PowerShell)
```powershell
# Убедитесь, что сервер запущен
powershell -ExecutionPolicy Bypass -File test-api-en.ps1
```

### Покрытие тестами
Проект включает 81 тест, покрывающий:
- CRUD операции с пользователями
- CRUD операции с вещами и валидацию владельца
- Жизненный цикл бронирования (создание → подтверждение/отклонение)
- Фильтрацию бронирований по состоянию
- Создание комментариев с валидацией бронирования
- Сценарии обработки ошибок

## 📁 Структура проекта

```
shareit/
├── src/
│   ├── main/
│   │   ├── java/ru/practicum/shareit/
│   │   │   ├── booking/          # Функционал бронирований
│   │   │   ├── config/           # Конфигурация OpenAPI
│   │   │   ├── exception/        # Глобальная обработка ошибок
│   │   │   ├── item/             # Функционал вещей и комментариев
│   │   │   ├── request/          # Функционал запросов на вещи
│   │   │   └── user/             # Функционал пользователей
│   │   └── resources/
│   │       ├── application.properties
│   │       ├── application-test.properties
│   │       └── schema.sql
│   └── test/
│       └── java/ru/practicum/shareit/
├── test-api-en.ps1               # Скрипт API-тестов
├── pom.xml
├── mvnw / mvnw.cmd
└── README.md
```

## ⚙️ Конфигурация

### application.properties
```properties
# База данных
spring.datasource.url=jdbc:postgresql://localhost:5432/shareit
spring.datasource.username=postgres
spring.datasource.password=postgres

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# Сервер
server.port=8080
```

### Переменные окружения
| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `DB_HOST` | localhost | Хост базы данных |
| `DB_PORT` | 5432 | Порт базы данных |
| `DB_NAME` | shareit | Имя базы данных |
| `DB_USER` | postgres | Пользователь БД |
| `DB_PASS` | postgres | Пароль БД |

## 🐳 Docker команды

```bash
# Запуск PostgreSQL
docker start shareit-postgres

# Остановка PostgreSQL
docker stop shareit-postgres

# Просмотр логов
docker logs shareit-postgres

# Подключение к базе данных
docker exec -it shareit-postgres psql -U postgres -d shareit

# Очистка всех данных
docker exec shareit-postgres psql -U postgres -d shareit \
  -c "TRUNCATE TABLE comments, bookings, items, users RESTART IDENTITY CASCADE;"
```

## 📝 Лицензия

Этот проект лицензирован под MIT License — см. файл [LICENSE](LICENSE) для деталей.

---

Английская документация: [README.md](README.md).
